
let mobile = {
    brand: 'Samsung',
    model: 'Galaxy Note 9'
};

for (let key in mobile) {
    console.log(`${key}: ${mobile[key]}`);
}

// The JavaScript for...in loop can be used to iterate over
// the keys of an object. In each iteration, one of the properties
// from the object is assigned to the variable of that loop.

